#include <bits/stdc++.h>
using namespace std;
int main(){
    /*int t;
    cin >> t;
    while(t--){
        string s;
        cin >> s;
        vector <int> a(s.size());
        a.push_back(0);
        for(int i = 0; i < s.size(); i++){
            if(s[i] == 'R')
                a.push_back(i + 1);
        }

        a.push_back(s.size() + 1);
        int m = 0;
        for(int i = 0; i < a.size() - 1; i++){
            m = max(m, a[i + 1] - a[i]);
        }

        cout << m << endl;
    }*/

    int s = pow(2, 3);
    cout << s << endl;

    return 0;
}
